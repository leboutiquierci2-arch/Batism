name: batismart
version: 0.1.0
publish_to: "none"

description: Application mobile pour devis BTP professionnels.

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.2
  intl: ^0.19.0
  pdf: ^3.11.0
  printing: ^5.13.0

flutter:
  uses-material-design: true
